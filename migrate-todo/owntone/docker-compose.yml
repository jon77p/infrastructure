version: "3.8"

services:
  owntone:
    image: ghcr.io/linuxserver/daapd@sha256:11fe2ab0b50e0a4afd048a92f986961d10e2c05bd93e24024d2f1725b13a2a9a
    hostname: "owntone"
    volumes:
      - /mnt/owntone/config:/config
      - /mnt/owntone/music:/music
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Los_Angeles
    networks:
      - host
    # deploy:
    # mode: global
    # restart_policy:
    # condition: any
    # window: 60s
    # placement:
    # constraints:
    # - node.role == manager
    # labels:
    # traefik.enable: "true"
    # traefik.http.routers.owntone.rule: Host(`owntone-cluster.${CF_DOMAIN}`)
    # traefik.http.routers.owntone.entrypoints: https
    # traefik.http.routers.owntone.tls.certresolver: cloudflare
    # traefik.http.services.owntone.loadbalancer.server.port: 3689

networks:
  host:
    name: host
    external: true
