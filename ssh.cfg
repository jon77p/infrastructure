IdentityFile ~/.ssh/oracle-terraform
IdentityFile ~/.ssh/picluster

Host pi1
  Hostname ssh-%h.thepi.cloud
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host pi2
  Hostname ssh-%h.thepi.cloud
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host innernet-server
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host timemachine
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host healthchecks
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host vault
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host gaps
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h

Host youtubedl
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h
