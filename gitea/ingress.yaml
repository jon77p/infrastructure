---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: gitea-http
  # namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`git-k3s.jon77p.xyz`)
    kind: Rule
    services:
      - name: gitea-http
        kind: Service
        port: 3000
