---
cloudflare_dns_records:
  - zone: "{{ domain }}"
    record: grafana.{{ domain }}
    type: CNAME
    value: tunnel-{{ ansible_host }}.{{ domain }}
    proxied: true
    state: present
  - zone: "{{ domain }}"
    record: influxdb.{{ domain }}
    type: CNAME
    value: tunnel-{{ ansible_host }}.{{ domain }}
    proxied: true
    state: present
