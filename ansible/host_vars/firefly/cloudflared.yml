---
additional_ingress:
  - hostname: "firefly.{{ domain }}"
    service: "http://localhost:{{ firefly_port }}"
  - hostname: "firefly-importer.{{ domain }}"
    service: "http://localhost:{{ firefly_fidi_port }}"
