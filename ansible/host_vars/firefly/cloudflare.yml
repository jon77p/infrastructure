---
cloudflare_dns_records:
  - zone: "{{ domain }}"
    record: "firefly.{{ domain }}"
    type: CNAME
    value: "tunnel-{{ ansible_host }}.{{ domain }}"
    proxied: true
    state: present
  - zone: "{{ domain }}"
    record: "firefly-importer.{{ domain }}"
    type: CNAME
    value: "tunnel-{{ ansible_host }}.{{ domain }}"
    proxied: true
    state: present
