---
helm:
  name: "{{ service_name }}"
  repo:
    name: "grafana"
    url: "https://grafana.github.io/helm-charts"
  chart: "grafana/loki"
  namespace: "{{ service_name }}"
  create_namespace: true
  chart_values: "{{ values }}"
