---
helm:
  name: "{{ service_name }}"
  repo:
    name: "grafana"
    url: "https://grafana.github.io/helm-charts"
  chart: "grafana/grafana"
  namespace: "{{ service_name }}"
  chart_values: "{{ values }}"
