---
iptables_rules:
  - action: insert
    chain: INPUT
    rule_num: 6
    ctstate: NEW
    destination_port: "{{ innernet_server_port }}"
    protocol: tcp
    state: present
    jump: ACCEPT
    comment: "Accept new connections to TCP/{{ innernet_server_port }}"
  - action: insert
    chain: INPUT
    rule_num: 7
    ctstate: NEW
    destination_port: "{{ innernet_server_port }}"
    protocol: udp
    state: present
    jump: ACCEPT
    comment: "Accept new connections to UDP/{{ innernet_server_port }}"
  - action: insert
    chain: INPUT
    rule_num: 8
    ctstate: NEW
    destination_port: "{{ crowdsec.prometheus.port }}"
    protocol: tcp
    state: present
    jump: ACCEPT
    comment: "Accept new connections to TCP/{{ crowdsec.prometheus.port }}"
  - action: insert
    chain: INPUT
    rule_num: 9
    ctstate: NEW
    destination_port: "{{ node_exporter.port }}"
    protocol: tcp
    state: present
    jump: ACCEPT
    comment: "Accept new connections to TCP/{{ node_exporter.port }}"
