---
oci_profile: "oci#"
oci_security_rules:
  egress_security_rules: []
  ingress_security_rules:
    - description: "allow a service TCP/22 inbound"
      source: "0.0.0.0/0"
      protocol: "6" # TCP
      tcp_options:
        destination_port_range:
          max: "22"
          min: "22"
