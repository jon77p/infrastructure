---
healthcheck_uuid: "{{ vault_healthcheck_uuid }}"

# docker-compose healthcheck snippet
#
# healthcheck:
#   test: [ "CMD", "curl", "https://healthchecks.{{ domain }}/ping/{{ healthcheck_uuid }}" ]
#   timeout: 45s
#   interval: 10s
#   retries: 5
#
