---
additional_ingress:
  - hostname: "{{ compose_project_name }}.{{ domain }}"
    service: "http://localhost:{{ compose_project_port }}"
  - hostname: "{{ compose_project_name }}.thepi.cloud"
    service: "http://localhost:{{ compose_project_port }}"
