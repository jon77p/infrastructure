---
version: "3.4"
services:
  vault:
    image: hashicorp/vault:latest@sha256:5cd2003247e0a574a66c66aee1916b1e9e7f99640298f2e61271a8842d2d2a19
    volumes:
      - "{{ docker_volume_dirs.vault[0] }}:/vault/config"
      - "{{ docker_volume_dirs.vault[1] }}:/vault/file"
      - "{{ docker_volume_dirs.vault[2] }}:/vault/logs"
      - "{{ docker_volume_dirs.vault[3] }}:/vault/plugins"
    ports:
      - "{{ vault.port }}:{{ vault.internal_port }}"
    cap_add:
      - IPC_LOCK
    restart: always
    command: [vault, server, -config, /vault/config/config.hcl]
  #   healthcheck:
  #     test: [CMD, wget, "{{ healthchecks.healthcheck }}", -O, /dev/null]
  #     timeout: 45s
  #     interval: "{{ healthchecks_defaults.ping.interval }}s"
  #     retries: 5
  opconnect_api:
    image: 1password/connect-api:latest@sha256:f97189814239381e6dd88577f2b0b838a64e006a460608455b3127c15b174601
    volumes:
      - "{{ docker_volume_dirs.opconnect[0] }}/1password-credentials.json:/home/opuser/.op/1password-credentials.json"
      - data:/home/opuser/.op/data
    ports:
      - "{{ opconnect.api.port }}:{{ opconnect.api.internal_port }}"
    restart: always
  opconnect_sync:
    image: 1password/connect-sync:latest@sha256:27e7ec47e1ad8eaa2f54764fa0736954a5119d0155dea3c923c481c89c5f964c
    volumes:
      - "{{ docker_volume_dirs.opconnect[0] }}/1password-credentials.json:/home/opuser/.op/1password-credentials.json"
      - data:/home/opuser/.op/data
    ports:
      - "{{ opconnect.sync.port }}:{{ opconnect.sync.internal_port }}"
    restart: always

volumes:
  data:
