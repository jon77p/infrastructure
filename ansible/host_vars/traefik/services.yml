---
services:
  - apiVersion: v1
    kind: Service
    metadata:
      name: "{{ service_name }}"
      namespace: "{{ service_name }}"
    spec:
      ports:
        - name: "{{ service_name }}"
          port: 80
          protocol: TCP
          targetPort: 80
      selector:
        app: "{{ service_name }}"
