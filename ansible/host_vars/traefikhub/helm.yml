---
helm:
  name: "{{ service_name }}"
  repo:
    name: "hub"
    url: "https://helm.traefik.io/hub"
  chart: "hub/hub"
  namespace: "{{ service_name }}-agent"
  create_namespace: true
  chart_values: "{{ values }}"
