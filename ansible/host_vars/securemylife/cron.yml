---
cronjobs:
  - name: HealthCheck
    minute: "*"
    hour: "*"
    day: "*"
    weekday: "*"
    month: "*"
    job: curl -m 10 --retry 5 -o /dev/null https://healthchecks.{{ healthcheck_domain }}/ping/{{ opconnect_results[inventory_hostname].healthcheck }}
    state: present
