---
cloudflare_dns_records:
  - zone: "{{ domain }}"
    record: "{{ codeserver.project_name }}.{{ domain }}"
    type: CNAME
    value: "tunnel-{{ ansible_host }}.{{ domain }}"
    proxied: true
    state: present
