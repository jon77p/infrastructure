---
helm:
  name: "{{ service_name }}"
  repo:
    name: "kodelib"
    url: "https://helm.kodelib.com"
  chart: "kodelib/code-server"
  namespace: "{{ service_name }}"
  create_namespace: true
  chart_values: "{{ values }}"
