---
- hosts: timemachine
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - stefangweichinger.ansible_rclone
    - rclone
    - docker
    - iptables

- hosts: localhost
  vars:
    oci_hostname: "timemachine"
  roles:
    - oci
