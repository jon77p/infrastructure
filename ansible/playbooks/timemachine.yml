---
- hosts: timemachine
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - stefangweichinger.ansible_rclone
    - timemachine
    - iptables

- hosts: localhost
  vars:
    oci_hostname: "timemachine"
  roles:
    - oci
