---
# todo: setup oci so that ingress rule can be added for timemachine service
# - hosts: localhost
  # collections:
    # - oracle.oci
  # vars:
    # config_profile: "oci1"

- hosts: timemachine
  become: true
  roles:
    - common
    - stefangweichinger.ansible_rclone
    - timemachine
  vars_files:
    - files/timemachine_vars.yml
