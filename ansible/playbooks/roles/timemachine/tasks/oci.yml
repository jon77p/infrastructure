- name: Get terraform compartment OCID
  oci_identity_compartment_facts:
    name: "terraform"
  register: compartments

- debug:
  var: compartments.compartments

# - name: Add ingress security rule
  # oci_network_security_list:
    # compartment_id: "todo"
    # ingress_security_rules:
    # - protocol: 6
      # description: "Allow timemachine access to {{ timemachine_port }} from anywhere"
      # source: "0.0.0.0/0"
      # tcp_options:
        # destination_port_range:
          # max: "{{ timemachine_port }}"
          # min: "{{ timemachine_port }}"
    # purge_security_rules: false
    # state: present
