[Unit]
Description={{ service_description }}
Documentation=
After=network.target

[Service]
Type=simple
User={{ user }}
ExecStart=/usr/local/bin/rclone mount gsuite: {{ mount_dir }} \
  --allow-other --allow-non-empty
ExecStop=/bin/fusermount -uzq {{ mount_dir }}
Restart=on-failure

[Install]
WantedBy=multi-user.target
