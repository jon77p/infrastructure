---
docker_volume_dirs: "{{ hostvars[inventory_hostname].docker_volume_dirs }}"
prometheus: "{{ hostvars[inventory_hostname].prometheus }}"
cadvisor: "{{ hostvars[inventory_hostname].cadvisor }}"
node_exporter: "{{ hostvars[inventory_hostname].node_exporter }}"
grafana: "{{ hostvars[inventory_hostname].grafana }}"
pihole_exporter: "{{ hostvars[inventory_hostname].pihole_exporter }}"
innernet_peers: "{{ hostvars['innernet'].oci_peers }}"
