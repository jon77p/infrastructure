---
- name: Get Healthcheck from 1Password-Connect
  vars:
    opconnect_item_names: "{{ hostnames }}"
    opconnect_field_names:
      - "healthcheck"
  include_role:
    name: "1password"

- when: inventory_hostname != 'localhost'
  set_fact:
    healthcheck_uuids: "{{ opconnect_results }}"
