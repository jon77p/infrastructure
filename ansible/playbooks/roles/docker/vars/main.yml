---
compose_project_name: "{{ hostvars[ansible_hostname].compose_project_name | default(None) }}"
compose_definition: "{{ hostvars[ansible_hostname].compose_definition | default(None) }}"
compose_service_names: "{{ hostvars[ansible_hostname].compose_service_names | default([]) }}"
servicelist: "{{ hostvars[ansible_hostname].servicelist | default([]) }}"
