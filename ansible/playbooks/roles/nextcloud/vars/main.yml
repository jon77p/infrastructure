---
nextcloud: "{{ hostvars[inventory_hostname].nextcloud }}"
docker_volume_dirs: "{{ hostvars[inventory_hostname].docker_volume_dirs | default({}) }}"
docker_user_id: "{{ hostvars[inventory_hostname].docker_user_id | default({}) }}"
docker_group_id: "{{ hostvars[inventory_hostname].docker_group_id | default({}) }}"
