---
- name: Install innernet via cargo
  vars:
    innernet_binary: innernet
    innernet_program_type: client
    cargo_path: /home/pi/.cargo
  ansible.builtin.include_tasks: innernet-install-cargo.yml
