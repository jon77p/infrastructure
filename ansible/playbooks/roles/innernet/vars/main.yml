---
innernet_network: "{{ hostvars[inventory_hostname].innernet_network }}"
innernet_network_cidr: "{{ hostvars[inventory_hostname].innernet_network_cidr }}"
innernet_server_port: "{{ hostvars[inventory_hostname].innernet_server_port }}"
innernet_network_groups: "{{ hostvars[inventory_hostname].innernet_network_groups }}"
missing_innernet_network_groups: []
