---
innernet_network: "{{ hostvars['innernet'].innernet_network }}"
innernet_network_cidr: "{{ hostvars['innernet'].innernet_network_cidr }}"
innernet_server_port: "{{ hostvars['innernet'].innernet_server_port }}"
cluster_network_name: "{{ groupvars['cluster'].cluster_network_name }}"
cluster_network_cidr: "{{ groupvars['cluster'].cluster_network_cidr }}"
cluster_network_parent: "{{ groupvars['cluster'].cluster_network_parent }}"
