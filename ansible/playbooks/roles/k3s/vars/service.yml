---
service_name: "{{ obj }}"
helm: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/helm.yml', errors='ignore') | from_yaml | default() }}"
namespace: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/namespace.yml', errors='ignore') | from_yaml | default() }}"
ingress: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/ingress.yml', errors='ignore') | from_yaml | default() }}"
deployment: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/deployment.yml', errors='ignore') | from_yaml | default() }}"
service: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/service.yml', errors='ignore') | from_yaml | default() }}"
secret: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/secret.yml', errors='ignore') | from_yaml | default() }}"
resources: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/resources.yml', errors='ignore') | from_yaml | default() }}"
dns_records: "{{ lookup('ansible.builtin.template', '../vars/{{ obj }}/dns-records.yml', errors='ignore') | from_yaml | default() }}"
