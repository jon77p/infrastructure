---
servicename: "{{ obj.key }}"
namespaces: "{{ obj.value.namespaces | default([]) }}"
deployments: "{{ obj.value.deployments | default([]) }}"
services: "{{ obj.value.services | default([]) }}"
ingress: "{{ obj.value.ingress | default([]) }}"
secrets: "{{ obj.value.secrets | default([]) }}"
additional_resources: "{{ obj.value.additional_resources | default([]) }}"
