---
name: "{{ service_name }}"
repo:
  name: "jetstack"
  url: "https://charts.jetstack.io"
chart: "jetstack/cert-manager"
namespace: "{{ service_name }}"
create_namespace: true
chart_values:
  installCRDs: true
