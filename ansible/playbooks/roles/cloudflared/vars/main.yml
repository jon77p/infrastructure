---
additional_ingress: "{{ hostvars[inventory_hostname].additional_ingress | default([]) }}"
cf_account: "{{ vault_cf_account }}"
cf_tunnel_id: "{{ hostvars[inventory_hostname].cf_tunnel_id | default(None) }}"
cf_tunnel_name: "{{ hostvars[inventory_hostname].cf_tunnel_name | default(None) }}"
cf_tunnel_secret: "{{ hostvars[inventory_hostname].cf_tunnel_secret | default(None) }}"
