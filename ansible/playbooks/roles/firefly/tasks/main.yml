---
- name: Create mount directories
  become: true
  file:
    path: "{{ path }}"
    recurse: true
    state: directory
  loop: "{{ docker_volume_dirs }}"
  loop_control:
    loop_var: "path"

- name: Create config file
  become: true
  when: "docker_volume_dirs != []"
  template:
    src: ../templates/config.toml.j2
    dest: "{{ docker_volume_dirs[2] }}/config.toml"
