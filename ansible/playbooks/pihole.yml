---
- hosts: localhost,pihole
  vars:
    hostname: "pihole"
  roles:
    - healthcheck

- hosts: pihole
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - pihole
    - docker
