---
- hosts: localhost,pihole
  vars:
    opconnect_item_names:
      - "pihole"
    opconnect_field_names:
      - "healthcheck"
      - "password"
  roles:
    - 1password

- hosts: pihole
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - innernet
    - pihole
    - promtail
    - docker
