---
### docker deployment
# - hosts: gaps
# become: true
# roles:
# - common
# - cloudflared
# - cloudflare
# - docker

- hosts: localhost
  vars:
    servicename: gaps
  gather_facts: false
  connection: local
  roles:
    - k8s

- hosts: manager
  roles:
    - cloudflared

- hosts: nodes
  roles:
    - cloudflared
