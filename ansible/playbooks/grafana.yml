---
- hosts: localhost,grafana
  vars:
    opconnect_item_names:
      - "grafana"
    opconnect_field_names:
      - "healthcheck"
  roles:
    - 1password

- hosts: grafana
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - grafana
    - docker
