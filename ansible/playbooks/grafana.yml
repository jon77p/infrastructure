---
- hosts: localhost,grafana
  vars:
    hostnames:
      - "grafana"
  roles:
    - healthcheck

- hosts: grafana
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - grafana
    - docker
