---
- hosts: localhost,grafana
  vars:
    hostname: "grafana"
  roles:
    - healthcheck

- hosts: grafana
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - grafana
    - docker
