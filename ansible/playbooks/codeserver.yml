---
- hosts: localhost,codeserver
  vars:
    hostnames:
      - "codeserver"
  roles:
    - healthcheck

- hosts: codeserver
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - codeserver
