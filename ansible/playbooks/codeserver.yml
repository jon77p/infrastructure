---
- hosts: localhost,codeserver
  vars:
    opconnect_item_names:
      - "codeserver"
    opconnect_field_names:
      - "healthcheck"
      - "password"
  roles:
    - 1password

- hosts: codeserver
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - innernet
    - dns
    - codeserver
