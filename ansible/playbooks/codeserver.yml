---
- name: Get 1Password secrets
  hosts: localhost,codeserver
  vars:
    opconnect_items:
      codeserver:
        - healthcheck
        - password
      crowdsec:
        - key
  roles:
    - 1password

- name: Setup codeserver
  hosts: codeserver
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - crowdsec
    - iptables
    - innernet
    - dns
    - node_exporter
    - codeserver
