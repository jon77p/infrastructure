---
- name: Get 1Password secrets
  hosts: localhost,vault
  vars:
    opconnect_items:
      vault:
        - healthcheck
  roles:
    - opconnect

- name: Setup vault
  hosts: vault
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    # - dns
    - vault
    - docker
