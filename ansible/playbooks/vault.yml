---
- hosts: localhost,vault
  vars:
    opconnect_item_names:
      - "vault"
    opconnect_field_names:
      - "healthcheck"
  roles:
    - 1password

- hosts: vault
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - innernet
    - dns
    - vault
    - docker
