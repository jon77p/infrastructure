---
- hosts: localhost,vault
  vars:
    hostnames:
      - "vault"
  roles:
    - healthcheck

- hosts: vault
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - vault
    - docker
