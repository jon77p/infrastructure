---
- hosts: localhost,firefly
  vars:
    hostnames:
      - "firefly"
  roles:
    - healthcheck

- hosts: firefly
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - docker
