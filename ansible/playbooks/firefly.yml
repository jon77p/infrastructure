---
- hosts: localhost,firefly
  vars:
    hostnames:
      - "firefly"
      - "firefly-importer"
  roles:
    - healthcheck

- hosts: firefly
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - docker
