---
- hosts: localhost,firefly
  vars:
    opconnect_item_names:
      - "firefly"
      - "firefly-importer"
    opconnect_field_names:
      - "healthcheck"
  roles:
    - 1password

- hosts: firefly
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - innernet
    - docker
