---
- hosts: localhost
  vars:
    servicename: "traefikhub"
  gather_facts: false
  connection: local
  roles:
    - k8s

- hosts: manager
  roles:
    - cloudflared

- hosts: nodes
  roles:
    - cloudflared
