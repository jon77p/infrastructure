---
- hosts: localhost,allaboutsecurity
  vars:
    opconnect_item_names:
      - "allaboutsecurity"
      - "ghost"
    opconnect_field_names:
      - "healthcheck"
      - "github_username"
      - "github_token"
  roles:
    - 1password

- hosts: allaboutsecurity
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - innernet
    - dns
    - ghost
    - promtail
    - docker
