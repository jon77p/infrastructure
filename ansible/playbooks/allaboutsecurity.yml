---
- hosts: localhost,allaboutsecurity
  vars:
    opconnect_item_names:
      - "allaboutsecurity"
    opconnect_field_names:
      - "healthcheck"
  roles:
    - 1password

- hosts: allaboutsecurity
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - innernet
    - dns
    - docker
