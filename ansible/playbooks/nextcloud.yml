---
- hosts: localhost,nextcloud
  vars:
    hostnames:
      - "nextcloud"
  roles:
    - healthcheck

- hosts: nextcloud
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - stefangweichinger.ansible_rclone
    - rclone
    - docker
