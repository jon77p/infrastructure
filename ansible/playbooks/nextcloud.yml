---
- hosts: localhost,nextcloud
  vars:
    opconnect_item_names:
      - "nextcloud"
    opconnect_field_names:
      - "healthcheck"
      - "username"
      - "password"
      - "smtp_username"
      - "smtp_password"
      - "mysql_username"
      - "mysql_password"
  roles:
    - 1password

- hosts: nextcloud
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - innernet
    - dns
    - stefangweichinger.ansible_rclone
    - rclone
    - docker
