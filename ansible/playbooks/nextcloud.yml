---
- hosts: localhost,nextcloud
  vars:
    opconnect_item_names:
      - "nextcloud"
    opconnect_field_names:
      - "healthcheck"
  roles:
    - 1password

- hosts: nextcloud
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - innernet
    - stefangweichinger.ansible_rclone
    - rclone
    - docker
