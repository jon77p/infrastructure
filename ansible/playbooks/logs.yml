---
- hosts: localhost,logs
  vars:
    opconnect_item_names:
      - "logs"
      - "grafana"
    opconnect_field_names:
      - "healthcheck"
      - "username"
      - "password"
      - "smtp_username"
      - "smtp_password"
  roles:
    - 1password

- hosts: logs
  become: true
  roles:
    # - common
    # - cloudflared
    # - cloudflare
    # - cron
    # - innernet
    # - dns
    - loki
    - promtail
    - grafana
    - docker
