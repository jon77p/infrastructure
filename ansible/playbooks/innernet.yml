---
- hosts: localhost,innernet
  vars:
    opconnect_items:
      innernet:
        - healthcheck
      crowdsec:
        - key
  roles:
    - 1password

- hosts: innernet
  become: true
  roles:
    - common
    - cloudflared
    - cloudflare
    - cron
    - crowdsec
    - iptables
    - innernet
    - iptables
    - dns
    - node_exporter

- hosts: localhost
  vars:
    hostname: innernet
  roles:
    - oci
