---
domain: "thepi.cloud"
cf_tunnel_id: "{{ vault_cf_tunnel_id }}"
cf_tunnel_name: "{{ vault_cf_tunnel_name }}"
cf_tunnel_secret: "{{ vault_cf_tunnel_secret }}"
pilot_token: "{{ vault_pilot_token }}"
