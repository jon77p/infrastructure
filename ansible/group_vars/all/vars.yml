---
user_password: "{{ vault_user_password }}"
cf_ssh_subdomain: "ssh-{{ inventory_hostname }}.{{ domain }}"
cf_tunnel_subdomain: "tunnel-{{ inventory_hostname }}.{{ domain }}"
cloudflare_dns_ip_addresses: "{{ vault_cloudflare_dns_ip_addresses }}"
