all:
  children:
    cluster:
      children:
        k3s:
          hosts:
            pi1:
              ansible_host: pi1
            pi2:
              ansible_host: pi2
          vars:
            ansible_ssh_user: pi
          children:
            manager:
              hosts:
                pi1:
            nodes:
              hosts:
                pi2:
    oci:
      children:
        services:
          hosts:
            firefly:
              ansible_host: firefly
            stash:
              ansible_host: stash
        innernet:
          hosts:
            innernet_server:
              ansible_host: innernet-server
      vars:
        ansible_ssh_user: ubuntu
