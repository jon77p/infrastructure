IdentityFile ~/.ssh/oracle-terraform
IdentityFile ~/.ssh/picluster

Host pi1
  Hostname ssh-%h.thepi.cloud
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host pi2
  Hostname ssh-%h.thepi.cloud
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host innernet-server
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host timemachine
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host healthchecks
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host vault
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host nextcloud
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host firefly
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log

Host stash
  Hostname 150.230.44.67
  IdentityFile ~/.ssh/oracle-stash.key

Host kube*
  Hostname ssh-%h.jon77p.xyz
  ProxyCommand cloudflared access ssh --service-token-id "$CF_ACCESS_SERVICE_TOKEN_ID" --service-token-secret "$CF_ACCESS_SERVICE_TOKEN_SECRET" --hostname %h --loglevel debug --logfile /tmp/ansible-cloudflared-log-%h-%p-%r.log
