ingress:
  main:
    enabled: true
    hosts:
      - host: nodered-k3s.jon77p.xyz
        paths:
          - path: /
persistence:
  config:
    enabled: true
  data:
    enabled: true
    accessMode: ReadWriteOnce
    size: 1Gi
